response 
{
    todos:[{
        "title" : "cleaning",
        "status": "pending"
    },{}]
}
path params vs key and values
controllers(data manipulation here) & handlers(only exposed to users,chron jobs)
status pending or complete
queries in db layer
post get contracts


django:
install new django app
export django settings